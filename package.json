{
  "name": "plumescope",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "concurrently": "9.2.1",
    "prettier": "3.6.2"
  },
  "scripts": {
    "build": "npm run --workspace apps/web build && npm run --workspace apps/api build",
    "dev": "concurrently -n web,api -c blue,green \"npm run --workspace apps/web dev\" \"npm run --workspace apps/api dev\"",
    "start": "concurrently -n web,api -c blue,green \"npm run --workspace apps/web start\" \"npm run --workspace apps/api start\""
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  }
}
